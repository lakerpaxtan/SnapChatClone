# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SnapchatProject' do
  pod 'Firebase/Core' 
  pod 'FirebaseAuth'
  pod 'FirebaseStorage' 
  pod 'SDWebImage'
  pod 'MBProgressHUD' 
  pod 'FirebaseDatabase'
end
